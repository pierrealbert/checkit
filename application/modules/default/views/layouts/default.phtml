<?
    $this->headLink()->headLink(array(
        'rel'   => 'favicon',
		'href'  => '/favicon.ico',
		'type'  => 'image/x-icon'
    ));

    $this->headLink()->appendStylesheet('/assets/css/chosen/chosen.css');
    $this->headLink()->appendStylesheet('/assets/css/style.css');
//	$this->headLink()->appendStylesheet('http://code.jquery.com/ui/1.10.3/themes/smoothness/jquery-ui.css');

    $this->headMeta()->appendHttpEquiv('Content-Type', 'text/html;charset=' . $this->getEncoding());
    $this->headScript()->appendFile('/assets/js/dropdown.js');
    $this->headScript()->appendFile('/js/jquery.form.js');
    $this->headScript()->appendFile('/assets/js/jquery.the-modal.js');
    $this->headScript()->appendFile('/assets/js/chosen.jquery.min.js');
    $this->headScript()->appendFile('/js/common.js');
    $this->headScript()->appendFile('/assets/js/app.js');
    $this->headMeta()->appendHttpEquiv('Content-Language', 'en');

    if (!empty($this->homeHeader)) {
        $this->headLink()->appendStylesheet('/css/ui/jquery-ui-1.10.3.css');
        $this->headLink()->appendStylesheet('/assets/css/search_form.css');
    }
?>
<?= $this->doctype() ?>
<html>
<head>
    <meta charset="UTF-8">
    <?= $this->headMeta() ?>
    <?= $this->headTitle('Frontend')->setSeparator(' - ') ?>
    <?= $this->headLink() ?>    
    <?= $this->jQuery() ?>
    <?= $this->headScript() ?>
</head>
    <body <?php echo (!empty($this->homeHeader) ? 'class="home"' : ''); ?>>
    	<!-- Success popup -->
        <div id="success-popup">
            <div class="success-popup-content">
                <h1>Compte supprimé</h1>
                <p>Votre compte a bien été supprimé. Vos données ont été effacées.<br>Nous sommes triste de vous voir partir.<br>Nous esperons vous revoir très vite sur Ommi.</p>
                <a href="#" class="btn btn-blue modal-close">Fermer</a>
            </div>
        </div>
		<!-- Default popup -->
        <div id="modal" style="display: none;">
            <div class="modal-title-container">
                <div class="modal-title">Inscription</div>
                <div class="modal-close"></div>
            </div>
            <div class="modal-content-container"></div>
        </div>
        
        <?= $this->partial('_partials/header.phtml', $this->getVars()) ?>

        <?php if(!empty($this->homeHeader)): ?>
            <?= $this->partial('_partials/promo.phtml', $this->getVars()) ?>
        <?php endif; ?>

        <main>
            <?= $this->messenger() ?>
            <?= $this->layout()->content ?>
        </main>
        <?= $this->partial('_partials/footer.phtml', $this->getVars()) ?>        
    </body>
</html>